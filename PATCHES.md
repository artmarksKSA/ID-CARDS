# بطاقات ArtMark — النسخة المُرقَّعة (قائمة منسدلة + جوال + الخط الرسمي)

`id-cards/index.html` — **305,689** بايت · `sha256 b471b9f85725995d1cc7197baef8e3330ae4eae7127647893675262479c9b3c8` (نفس الملف الذي فُحص في المتصفح).
مبنيّة من `index.html` المنشور في `artmarksKSA/ID-CARDS`. صور القوالب التسعة تُقرأ من المستودع كما هي — **لا ملفات جديدة** سوى ملف الخط (انظر §٢).

---

## ١) الخط الرسمي (المطلوب في هذه الجولة)

الخط الرسمي مُعرَّف باسم ثابت واحد: **`Mohammad Bold Art`** وكل شي مربوط به:

| أين | ماذا صار |
|---|---|
| `@font-face` | إن وُجد ملف الخط وقت البناء ضُمِّن **base64 داخل index.html** (يبقى المشروع ملفاً واحداً ولا يطلب أي شبكة)؛ وإن لم يوجد أشار إلى `fonts/mohammad-bold-art-1.ttf`. |
| واجهة البرنامج | `--ui-font` يبدأ بالرسمي، وبعده IBM Plex Sans Arabic / Tajawal / Segoe UI كاحتياط لكل حرف ناقص. |
| نص جديد | زر «نص» يولّد على الرسمي (كان `Cairo`). |
| قائمة الخطوط بالمزايا | الرسمي أول بند و**مُختار** افتراضياً؛ بقية الخطوط باقية لمن يريد تغيير طبقة بعينها. |
| حقول القالب (الاسم/الرقم/الانتهاء) | تبنى على الرسمي. |
| التخطيطات المحفوظة قديماً على Cairo/Tajawal | تُرقَّى للرسمي **مرة واحدة** (`artmark.layouts.v1.fontv`)، والمواضع تبقى كما هي. |
| قبل التصدير | يُنتظر `document.fonts` ثم تُعاد قياس طبقات النص، فلا تخرج البطاقة بخط بديل عند أول تشغيل. |

### ما ينقص لإكمالها: الملف نفسه
`mohammad-bold-art-1.ttf` **ليس في مساحة العمل** (لم يصل مرفقاً)، لذلك النسخة المسلَّمة تشتغل بوضع «المسار النسبي». اختر أحد طريقين:

1. **ارفعيه مع الموقع**: ضعي الملف في المستودع على `fonts/mohammad-bold-art-1.ttf` (بجانب index.html). لا تعديل آخر مطلوب.
2. **أرفقنيه هنا** وسأعيد البناء بوضع التضمين: ملف واحد بحجم ‎+120–380 KB (حسب حجم الخط ×1.37)‎ تقريباً، ولا طلب شبكة للخط إطلاقاً (هذا ما فُحص في البندين ١ و٧ من `test_font.py`، وبخط بديل 91 KB لاختبار الآلية فقط).

الضبط كله في `id-cards/src/font.json` (اسم العائلة، الاسم الظاهر، المسار، أوزان، `embed`, قائمة الخطوط القديمة المراد ترقيتها) — تغيير اسم العائلة فيه يُحدِّث كل شي عند إعادة البناء.

> إن كان للملف أوزان متعددة (عادي/عريض) أو كان `woff2`/متغيّراً، أخبريني: تُضاف وجوه `@font-face` بأنطاق أوزان، أو أُبدّل `src` بـ `format('woff2')`.

---

## ٢) قائمة البطاقات منسدلة + توافق الجوال (جولة سابقة، ما زالت كما هي)
- **قائمة `قالب البطاقة`**: مجموعة `خاصة · عضويات الجمعية (9)` ثم `عامة · قوالبك في هذا المتصفح (n)` ثم `＋ إضافة قالب من جهازك…` و`✕ حذف هذا القالب`، وبند إرشادي أولي. سطر حالة تحته: من المستودع/من جهازك + «إرجاع المواضع».
- النقر على قالب يبني الخلفية + أربع طبقات مرتبطة (صورة العضو دائرية تُقصّ لتملأ، الاسم، رقم العضوية، تاريخ الانتهاء) بمواضع مقيسة من نفس الصورة وبلون مناسب لخلفية كل قالب.
- **منسدلة السجلات** في شريط المعاينة: «الاسم · رقم العضوية» لكل سجل، ومتزامنة مع الأسهم.
- **تحديد المواضع**: شريط عائم على الجوال («الخطوة 2 من 4 — موضع الاسم» + زر إنهاء) وأربع نقرات بالترتيب.
- كل قالب يحفظ تخطيطه في المتصفح؛ `templates.json` يصدّرها ليرفع مع المستودع.
- أحجام لمس ≥44–48px، خط 16px في القوائم (يمنع تكبير iOS)، بلا فائض أفقي عند 360/390، وتكبير المعاينة يعمل (رقعتا `E1`+`E2`).

### الافتراض الذي بُني عليه التقسيم
**خاصة** = التصاميم التسعة المودعة في المستودع. **عامة** = قوالبك المرفوعة المحفوظة في المتصفح (IndexedDB). إن كنتِ تقصدين «الخاصة بمفتاح دخول» أو «نفس التصاميم بلا درجة عضوية»، فالتبديل محصور في بناء مجموعات `#amTplSel` (سطران) — طلبك يكفيه سطر واحد مني.

---

## ٣) الرقع على كودك (21)

كل رقعة مشروطة بوجود نصّها مرة واحدة بالضبط في الأصل، والمُرقِّع يتوقف بلا كتابة إن لم يجده؛ الأرقام من `index.html` الأصلي. قائمة المنسدلة والجوال في طبقة `am-*`، ورقع `F*` وحدها تلمس كودك من أجل الخط.

| # | الرقعة | سطرُها في index.html الأصلي | ما تُصلحه |
|---|---|---|---|
| 1 | `E1-css-zoom` | 583 | **تكبير الجوال كان ميتاً.** على الجوال كانت قاعدة !important تلغي تكبير المعاينة (JS يكتب style.transform عادياً فلا يعلو عليها). الآن يمرّ التكبير عبر متغيّر CSS. |
| 2 | `E2-js-zoom` | 2086 | **تمرير المعامل للمتغيّر.** يُمرَّر نفس المعامل للمتغيّر الذي تقرأه قاعدة الجوال. |
| 3 | `E3-xlsx-raw` | 1403 | **أصفار الأرقام والتواريخ في Excel.** raw:false يحفظ «0501234567» و«0123…» كما هي بدل تحويلها أرقاماً تُفقد الأصفار، والتواريخ لا تصير 45000. defval:'' يضمن أن الخلية الفارغة تمسح قيمة السجل السابق بدل إرثها. |
| 4 | `E4-capture-freeze` | 1891 | **المعاينة كانت تُلوِّث «الأصل».** المعاينة الحية كانت تكتب بيانات السجل فوق القالب، فيُلتقط «الأصل» ملوَّثاً ويرجع السجل الأول بدل التصميم. |
| 5 | `E5-capture-store` | 1256 | **تجميد لقطة التصميم.** يجمّد لقطة التصميم الحقيقية أول مرة؛ تُبطَل تلقائياً عند أي تعديل (في طبقة E1). |
| 6 | `E6-revert-guard` | 1911 | **انهيار عند تغيّر عدد الطبقات.** لو تغيّر عدد العناصر أثناء التوليد كان يفشل بـ undefined.type ويترك الكانفس معلَّقاً. |
| 7 | `E7-preview-restore` | 1959 | **رجوع القالب قبل كل معاينة.** يرجع القالب لأصله قبل كل معاينة، فلا يبقى السجل السابق مطبوعاً على التصميم. |
| 8 | `E8-image-reset` | 1566 | **إرث صورة العضو السابق.** كل صورة مرتبطة تُعاد للعنصر النائب قبل التحميل، فلا ترث البطاقة التالية صورة العضو السابق. |
| 9 | `E9-image-miss` | 1869 | **سجلّ الصور غير المطابقة.** يسجّل الأسماء التي لم تجد صورة في المجلد ليُخبَر بها المستخدم بدل دائرة فارغة أو صورة خطأ. |
| 10 | `E10-miss-out` | 1887 | **تمرير السجل لحلقة التصدير.** يُمرَّر سجلّ النقص إلى حلقة التصدير. |
| 11 | `E11-zip-name` | 1937 | **تنقية أسماء الملفات ومنع التصادم.** تنقية الاسم (\ / : * ? " < > | والمسافات الخفية) ومنع التصادم بـ -2 بدل استبدال الملف بصمت ⇒ لا يسقط سجل من الـ ZIP. |
| 12 | `E12-zip-init` | 1928 | **انتظار جهوزية الخطوط.** ينتظر جهوزية الخطوط قبل أول لقطة/تصيير، وإلا خرج النص العربي بالحرف الاحتياطي في أول تشغيل أو بعد خط مرفوع. |
| 13 | `E13-zip-warn` | 1948 | **التنبيه بعد التنزيل.** يُعلن عن السجلات الناقصة بعد التنزيل، لا أثناءه، بأمثلة أول سطرين. |
| 14 | `E16-ogimage` | 9 | **صورة معاينة المشاركة (404).** كان يشير إلى assets/logo-140.png وهو 404 (لا يوجد assets/ في المستودع) ⇒ معاينة المشاركة بلا صورة. الآن صورة بطاقة موجودة فعلاً. |
| 15 | `E17-photo-cover` | 1876 | **صورة العضو تملأ الدائرة.** صورة العضو داخل إطار دائري تُملأ وتُقصّ أطرافها (cover)؛ contain وحده يُصغّر الصورة فتظهر هوامش داكنة داخل الدائرة. بقية الحقول تبقى على contain كما هو. |
| 16 | `F1-fontface` | 19 | **تعريف الخط الرسمي.** يُعرِّف الخط الرسمي بـ @font-face: إن وُجد ملف الخط بجانب المُرقِّع ضُمِّن base64 داخل الملف (يبقى المشروع ملفاً واحداً)، وإلا أشار إلى المسار في `src`. مع preload حين يكون خارجياً. |
| 17 | `F2-uifont` | 79 | **الخط الرسمي لواجهة البرنامج.** الخط الرسمي يصير خط الواجهة كله (الأزرار واللوحات)، وبقية Stack احتياطٌ لكل حرف ناقص. |
| 18 | `F3-default-text` | 1449 | **كل نص جديد يولد بالرسمي.** أي نص جديد يُضاف للبطاقة يولد على الخط الرسمي بدل Cairo. |
| 19 | `F4-font-list` | 1120 | **الرسمي أول بند في قائمة الخطوط.** الخط الرسمي يظهر أولاً في قائمة الخطوط بالمزايا ومختاراً كافتراضي لكل طبقة نص. |
| 20 | `F5-canvas-epoch` | 1354 | **وسم حقبة لإعادة تهيئة الكانفس.** كل رفع خلفية يبني كانفس جديداً (initCanvas يستبدل القديم) — الوسم يسمح لطبقة القوالب بالانتظار حتى يصير الكانفس الجديد جاهزاً، بدل أن ترى خلفية القالب السابق وتبني فوق كانفس سيُرمى. |
| + | `E14-cf-beacon` | وسم داخل `<body>` | **حذف وسم Cloudflare.** يُزال بتعبير نمطي في `main()` (لا بوصلة نص) لأن وسْمَه طويل وغير مستقر. |

---

## ٤) ملفات التسليم
| المسار | ما هو |
|---|---|
| `id-cards/index.html` | الملف الكامل المُرقَّع — يُرفع مكان `index.html` في المستودع. |
| `id-cards/fonts/` | ضعي هنا `mohammad-bold-art-1.ttf` (أو ارفعيه على نفس المسار في المستودع). |
| `id-cards/PATCHES.md` | هذا الشرح. |
| `id-cards/src/patch_idcards.py` | المُرقِّع + طبقة الخط: `python3 src/patch_idcards.py <index.html الأصلي> <مجلد المخرجات>`. |
| `id-cards/src/font.json` | ضبط الخط الرسمي (اسم العائلة/المسار/تضمين/ترقية الخطوط القديمة). |
| `id-cards/src/check_font.py` | فاحص ملف الخط: الاسم الداخلي، التغطية (عربي/أرقام/لاتيني)، الجداول، الوزن، هل هو متغيّر، حجم التضمين المتوقع. |
| `id-cards/src/snippets/gallery.css` · `gallery.js` | طبقة الواجهة لوحدها لمن تفضّل اللصق اليدوي قبل `</head>` و`</body>`. |
| `id-cards/src/test_*.py` | فحوص المتصفح الحقيقي الخمسة (تحتاج `pip install playwright` وخادماً محلياً + صور القوالب بجانب الملف). |
| `id-cards/previews/` | لقطات مكتب/جوال + بطاقة صُدِّرت من الأنبوب. |

---

## ٥) فُحِص في متصفح حقيقي — لا ادعاءات بلا تشغيل
Chromium بلا واجهة + سياق جوال بلمس حقيقي، ملف Excel حقيقي (٦ سجلات: اسم مكرر، اسم فيه `/` و`*`، صف بلا اسم، خانة `0000`، تاريخ حقيقي)، ومجلد صور ٣ (واحد مفقود عمداً). نسختان مبنيتان: **بالخط المضمَّن** (ملف خط حقيقي 91 KB لاختبار الآلية) و**بوضع المسار** (ملف الخطط غير مرفوع بعد — أي نسخة التسليم الحالية).

| الحزمة | الفحوص | النتيجة |
|---|---|---|
| `test_e2e.py` — القالب/التصدير/ZIP/الحفظ + ٤ تبديلات متتالية | 33 | 0 فشل (على النسختين، و٣ تكرارات متتالية) |
| `test_pub.py` — القوالب العامة + IndexedDB + `templates.json` + الحذف | 13 | 0 فشل |
| `test_mobile.py` — 390×844 و360×780 باللمس | 14 | 0 فشل |
| `test_font.py` — الخط: التحميل، الافتراضيات، قائمة الخطوط، الرسم، التصدير، الترقية | 10 | 0 فشل (نسخة التضمين) |
| `test_nofont.py` — سلوك نسخة التسليم بلا ملف الخط | 6 | 0 فشل |

أبرز الأرقام: `document.fonts.check` = true والخط حُمِّل **مع حجب كل شبكات الخطوط** (دليل التضمين) · رسم «12345 Abcدهي» بالملف ≠ رسم Arial ≠ monospace · بطاقة `1219×752` تغيّرت بياناتها (731,302 ↔ 733,046 حرفاً) عند تبديل الرسمي/Arial ⇒ الرسم يعتمد عليه فعلاً · الطبقات الثلاث `fontFamily = الرسمي` · `0123` بقيت «0123» والتاريخ «2026-01-01» · ZIP ستة ملفات بأسماء منقّاة ومكفولة التفرّد · بلا ملف الخط: النص يظهر بالاحتياطي بعد فترة الحجب ولا يعلق، ومحاولة جلب واحدة فقط، ولا أخطاء JS (فقط 404 متوقَّع).

### أخطاء حقيقية كشفها الفحص وأُصلحت في هذه الجولة
1. **البناء على كانفس مُلقى**: كل رفع خلفية يستدعي `initCanvas` الذي يستبدل نسخة فابريك؛ فكان الطبقات تُبنى على النسخة السابقة وتختفي عند اختيار قالب ثانٍ ⇒ وسم «حقبة» (`F5`) تنتظر الانتظار طبقات الكانفس الجديد، والفحص الآن يبدّل أربعة قوالب متتالية ويطلب ٤ طبقات في كل مرة.
2. **الحذف في IndexedDB كان قد يعلّق**: `onabort` لم يكن معالَجاً فبقي الانتظار بلا نهاية (يبدو الحذف ناجحاً ثم يعود القالب بعد التحميل) ⇒ صفقات مغلَقة بمعرِّف واحد `oncomplete/onabort/onerror` + مهلة أمان + إغلاق الاتصال، والتحقق من حذف فعلي وإعادة المحاولة.
3. **رفض غير معالَج عند غياب ملف الخط**: `document.fonts.load` ترفض بخطأ شبكة فكان يظهر `A network error occurred` في الطرفية عند كل فتح ⇒ ابتلاع الرفض وإرجاع false.
4. **وميض/سباق قائمة القوالب**: كانت تُعاد بنائها بعد كل اختيار ⇒ مقارنة توقيع القائمة وتخطي البناء المطابق + طابور يمنع تداخل بناء قالبين (`pick2`).

---

## ٦) النشر (٣ خطوات)
1. ارفعي `id-cards/index.html` مكان `index.html` في جذر المستودع، و`id-cards/fonts/mohammad-bold-art-1.ttf` على `fonts/…` (أو أرفقيه ليُضمَّن).
2. افتحي `https://artmarksksa.github.io/ID-CARDS/` واختباري على الجوال: قائمة القالب → ربط Excel → اختيار سجل بالاسم → توليد ZIP.
3. اختياري: `templates.json` بجانبه ليبدأ الجميع على المواضع التي ضبطتِها.

> QR لكل عضو ما زال غير مولَّد (الرمز جزء من صورة التصميم). وكل ملفات الأعضاء تبقى على جهازك: لا يُرفع Excel ولا الصور على أي خادم.
