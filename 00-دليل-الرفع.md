# دليل الرفع على GitHub Pages — `ID-CARDS-github.zip` (67.5KB)

## الطريق الأسهل: متصفح (يعمل من الجوال أيضاً)
1. نزّل `ID-CARDS-github.zip` من المساحة، **وفكّه** على جهازك ⇒ تحصل على `index.html` + مجلد `css` + مجلد `assets` + `README.md`.
2. افتح <https://github.com/artmarksksa/ID-CARDS> → زر **Add file** ▾ → **Upload files**.
3. اسحب محتويات المجلد **كاملة** (وليس الملف وحده) → **Commit changes**.
   - لو سأل «Replace existing files?» للملف `index.html` → موافق (النسخة القديمة محفوظة في سجل المستودع).
4. **Settings** → **Pages** → تأكد: Source = **Deploy from a branch**، Branch = `main` / **`/(root)`** → **Save**.
5. انتظر ١–٣ دقائق، ثم افتح <https://artmarksksa.github.io/ID-CARDS/> واضغط **Ctrl+Shift+R** (تحديث يتجاوز الكاش).

> ⚠️ لا ترفع `index.html` وحده. `css/theme.css` و`assets/…` مطلوبة بنفس الترتيب — الروابط نسبية.

## بديل نظيف: مستودع جديد
`New repository` باسم `artmark-cards` ← **Add a README** فقط ← ثم خطوات 2–5 أعلاه. رابطك يصير `artmarksksa.github.io/artmark-cards`.

## من الحاسوب بـ Git (الأسرع للتحديثات)
```bash
cd /path/to/ID-CARDS          # مجلد الموقع (index.html في جذره)
git init && git remote add origin https://github.com/artmarksksa/ID-CARDS.git
git add index.html css assets README.md .gitignore
git commit -m "D1: ثيم Obsidian+Accent، شعار في الرأس، توافق جوال، Toast بدل alert"
git push -f origin main       # -f لاستبدال index.html القديم
```

## لو ظهر الموقع بلا تنسيق
- 404 لـ`css/theme.css` ⇒ المجلد مرفوع في مكان خاطئ؛ لازم يكون `css/theme.css` في **جذر** المستودع لا داخل مجلد فرعي.
- تنسيق قديم ⇒ الكاش: **Ctrl+Shift+R**، أو أضف `?v=2` لرابط الصفحة مرة.
- Pages معطّل ⇒ Settings → Pages → Source: `Deploy from a branch`.

## أمان (مستودع عام — اقرأها)
- **لا ترفع**: ملفات Excel فيها أسماء/أرقام موظفين، ولا صور الأشخاص، ولا كلمات مرور. ارفع الكود فقط.
- نظام الدخول مؤجّل؛ ولو أضفناه لاحقاً في كود عام فهو **ردع** لا حماية. للحماية الحقيقية انقل إلى Cloudflare Pages + Zero Trust Access (يأتي في D7).
- حذف `index.legacy.html`: النسخة الأصلية ما زالت محفوظة في سجل git — `git show a45b52c:index.html > index-old.html` تستخرجها متى شئت.

## ملفات التطوير (لا تُرفع للموقع)
`ID-CARDS-dev/` فيها: `tools/build_ui.py` (البنّاء) · `templates/` (مصدر الـ markup + الأيقونات) · `index.legacy.html` · التقارير · اللقطات.
تعدّل `templates/index.src.html` أو `css/theme.css` ثم تشغّل `python3 ID-CARDS-dev/tools/build_ui.py` فيُعاد توليد `ID-CARDS/index.html`. إن أردتها على GitHub لمراجعتها لاحقاً، ارفعها في مجلد `dev/` منفصل داخل المستودع — لا تؤثر على الموقع.
